version: '3.8'

services:
  azbrief:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - LOG_LEVEL=DEBUG
    volumes:
      - ./src:/app/src:ro
    restart: unless-stopped
