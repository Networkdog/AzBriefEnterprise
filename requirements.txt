# Core
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
python-dotenv>=1.0.0,<2.0

# LangChain
langchain-core>=0.1.0,<1.0
langgraph>=0.0.20,<1.0

# Azure SDKs
azure-identity>=1.15.0,<2.0
azure-mgmt-resourcegraph>=8.0.0,<9.0
azure-communication-email>=1.0.0,<2.0
azure-mgmt-costmanagement>=4.0.0,<5.0
azure-monitor-query>=1.2.0,<3.0
azure-monitor-ingestion>=1.0.0,<2.0

httpx>=0.26.0,<1.0

# RSS Parsing
beautifulsoup4>=4.12.0,<5.0

# Utilities
structlog>=24.1.0,<26.0

# Microsoft Foundry backend (default) — hosted multi-agent pipeline, invoked
# through the Agents data plane. The code degrades to Azure OpenAI when the
# project endpoint is unreachable, but the SDK ships with the image so the
# default path works out of the box. Both are still beta, so the lower bound is
# the version this was verified against.
azure-ai-projects>=2.5.0,<3.0

# OpenTelemetry export to Application Insights (OTEL_ENABLED=true)
azure-monitor-opentelemetry>=1.6.0,<2.0
